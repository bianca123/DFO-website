{% extends "base.twig" %}

{% block content %}
<div class="page-header" style="background-image: url('{{post.thumbnail.src}}');"></div>
	<div class="container page-container">
	
		<div class="row">
			<div class="col-md-9">
	
				<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
					<section class="article-content">
						<h1 class="article-h1">{{post.title}}</h1>
						<div class="article-body">
							{{post.content}}
						</div>
					</section>
				</article>
			</div>
			<div class="col-md-3">
				<div class="sidebar">
					<h4>Ge&iuml;nteresseerd?</h4>
					<p>Bent u ge&iuml;nteresseerd in of heeft u een vraag over onze diensten?</p>
					<p>Vul hieronder uw naam, het onderwerp en telefoonnummer in en wij bellen u z.s.m. terug.</p> 
					{{ function('do_shortcode', '[contact-form-7 id="846" title="Terugbellen"]') }}
				</div>
			</div>
		</div>
	</div><!-- /content-wrapper -->
{% endblock %}