<ul class="products">
{% for post in post.get_field('producten_home') %}
	<li>
		<div class="row">
			<div class="col-md-6">
				<img src="{{ post.thumbnail }}" border="0" alt="{{ post.title }}" />
			</div>
			<div class="col-md-6">
				<h3>{{ post.title }}</h3>
				<p class="price">€495.00 excl. btw<p>
				<a target="{{ post.target }}" href="{{ post.link }}">Bekijk product <i class="fa fa-angle-right" aria-hidden="true"></i></a>
			</div>
		</div>
	</li>
{% endfor %}
</ul>
<ul class="dots">
{% for post in post.get_field('producten_home') %}
	<li>
		<a target="{{ post.target }}" href="{{ post.link }}"><i class="fa fa-circle" aria-hidden="true"></i></a>
	</li>
{% endfor %}
</ul>
